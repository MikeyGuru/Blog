<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
<%= favicon_link_tag 'favicon.ico' %>
    <title><%= content_for?(:title) ? yield(:title) : "Blitter" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Indie+Flower' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="stylesheets/animations.css">

  </head>

  <body>

<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "Blitter", root_url %></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if logged_in? %>

      <li><%= link_to "Logged in as #{current_user}", "#" %></li>
          <li><%= link_to "Your Post", posts_path %></li>
       <li><%= link_to "Logout", logout_path %></li>
    <% else %>
            <li><%= link_to "Login", new_login_path %></li>
            <li><%= link_to "Sign-up", new_user_path %></li>
    <% end %>
          </ul>
      </li>
    </ul>

    <!-- Left Nav Section -->
    <ul class="left">
    <li><%= link_to "New Post", new_post_path %></li>
    </ul>
  </section>
</nav>

<h1 id="logo" class="tossing">Blitter</h1>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
